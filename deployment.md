Agentic read agent (Phase 1) + V0.2 guided write workflows (Phase 2)

Context

Two connected goals:

Turn the fixed-context assistant into a proper agentic agent: understands whateveris asked, fetches data from the DB via read tools, handles normal chat, and                                                                                                                                                                                           ↓remembers the conversation (multi-turn).                                                                                                                                                                                                                              ↓

V0.2 — OpsGenie becomes source of truth: create invoices and record paymentsthrough WhatsApp, so data is fresh because OpsGenie generated it.↓Key design principle (from the user's Phase-2 review): writes are workflows, not AIcapabilities. The AI only makes them conversational — it must not extract line items /qty / unit / GST / due date, which is the highest-risk thing possible (a wrong invoice is areal business problem). Instead, every write is a deterministic guided flow that asks for                                                                                                                                                                             ↓each field explicitly (like the existing onboarding state machine), ends in a preview, andcommits only on confirm. The workflow works even without AI; the agent just recognizes                                                                                                                                                                                ↓intent and starts it.↓The money rule holds: LLM never invents/computes money. Reads → deterministic tools + atraceability gate. Writes → the user supplies every field via the guided flow, Python does                                                                                                                                                                            ↓l arithmetic, and a confirmation gate commits.

ovider reality (.env): chain openrouter → gemini → groq (anthropic keyless). No                                                                                                                                                                                     ↓provider does tool calling yet — adding it is Phase 1's core. Tool execution stays in ourPython, so llm/base.py's DB boundary holds.↓Build as two sequential, separately-reviewed phases.

---                                                                                                                                                                                                                                                                   ↓Phase 1 — Agentic read agent + multi-turn memory↓1.1 Conversation memory (app/models/conversation_turn.py + migration)↓nversationTurn: company_id (FK cascade), role ("user"|"assistant"), content (Text),eated_at; index (company_id, created_at). Store only dialogue turns (not tool calls);ent re-derives tools each message against current DB state. Load last N                                                                                                                                                                                             ↓GENT_HISTORY_TURNS, default 10); append user msg + final reply after answering.

2 Read tools (app/services/agent/read_tools.py)↓Registry, each tool = name + description + JSON-schema + async (db, company, **args) -> dict,tagged permission="read" (see 2.4). Reuse existing services: get_business_summary                                                                                                                                                                                     ↓uild_snapshot), get_cash_position, get_party_balance(name) (fuzzy dealer/supplier +lculate_party_outstanding), list_top_debtors/list_top_creditors                                                                                                                                                                                                     ↓(calculate_outstanding_for_company), list_overdue_dealers, get_upcoming_collections/get_upcoming_payments, list_products. JSON-safe, stringified Decimals.                                                                                                                                                                                                ↓

1.3 Runner + provider tool-calling↓p/services/agent/runner.py: run_agent(system_prompt, history, tools, execute, settings) -> AgentResult(text, tool_outputs, provider, model) — mirrors generate_with_fallback chaingic, drives a tool loop bounded by AGENT_MAX_STEPS (default 5). New optionalMProvider.run_tool_loop(...) (raises ToolCallingUnsupportedError by default), implemented                                                                                                                                                                           ↓for OpenAI-compatible (shared Groq/OpenRouter: tools=…, tool_choice="auto", loop onmessage.tool_calls) and Gemini (function_declarations, loop on function_calls).Runner skips no-key/unsupported/retryable providers.                                                                                                                                                                                                                  ↓

1.4 Money-safety gate (app/services/agent/money_guard.py)↓Extract _reply_has_unverified_money here. Known amounts = union of all tool outputs; any₹/Rs/INR/comma-grouped/bare-5+-digit figure in the reply not present → discard, safe                                                                                                                                                                                  ↓llback. Chat passes trivially.

5 Rewrite answer_question (same entry point, never-raises)                                                                                                                                                                                                          ↓

Phase 2 — V0.2 guided write workflows (invoice + payment)                                                                                                                                                                                                             ↓

2.1 Generic confirmation: PendingOperation (app/models/pending_operation.py + migration)                                                                                                                                                                              ↓

PendingOperation: id, company_id (FK cascade), operation_type (enum:create_invoice, record_payment, … room for add_dealer/add_supplier/cancel_invoice),                                                                                                                                                                                   ↓yload (JSONB), created_at, expires_at. One row = one validated write awaiting YES.single execute_pending_operation(db, company, op) dispatches by type to the deterministicite service. This is the reusable confirm mechanism for every future write type.                                                                                                                                                                                    ↓

2.2 Deterministic write services (app/services/writes/)↓

record_payment(db, company, , direction, party_name, amount, payment_date) — reuse theproven FIFO allocator in importer/payment_row.py (_allocate_payment_fifo), refactored toaccept a source param → PaymentSource.whatsapp. Already: find-or-create party →                                                                                                                                                                                       ↓dest-first allocation → status flips → payment_received events; raises on-open-invoice / exceeds-outstanding.create_invoice(db, company, , direction, party_name, line_items, due_date, gst_rate) —                                                                                                                                                                             ↓create Invoice + InvoiceItems (both whatsapp sources exist). Python computesline_total = qtyunit_price, subtotal = Σ, gst = subtotalrate, total = subtotal+gst.invoice_number = f"WA-{…}". Product matched to catalogue (optional product_id). Emits an                                                                                                                                                                              ↓voice_created event.

3 Guided flows (app/services/workflows/ — same state-machine shape as onboarding_flow)                                                                                                                                                                              ↓

mpany gains generic active_workflow (str|null) + workflow_scratch (JSONB) + migrationne flow active at a time). Zero AI reasoning inside — each field asked explicitly:                                                                                                                                                                                  ↓

Create-invoice flow: Dealer? → Product? → Quantity? → Price? → (another item? ) → GST%? →Due date? → Preview (itemized, computed total) → creates a PendingOperation and asks"Reply YES to create". Each field validated deterministically; product/dealer matched to                                                                                                                                                                              ↓existing records (offer to add if unknown).

Record-payment flow: Party? → Amount? → (date?) → Preview → PendingOperation → YES.                                                                                                                                                                                 ↓

After a successful invoice create → auto-offer: "✅ Invoice WA-123 created. Send it toRam Traders on WhatsApp? 1 Yes 2 No." Yes → send a formatted invoice message to the dealer'ssaved number (fail-open; note the WhatsApp 24h/template constraint — a dealer who never                                                                                                                                                                               ↓messaged us needs an approved template, so first cut logs/So-notes when it can't deliver).

2.4 Read/Write tool separation + permissions

Two registries: read tools (1.2) and write "starter" tools (start_create_invoice,                                                                                                                                                                                     ↓start_record_payment) tagged permission="write". The agent recognizes write intent andcalls a starter, which sets active_workflow and returns the flow's first question — then the                                                                                                                                                                          ↓deterministic flow takes over. A single permission tag per tool is the seam for futurerole-based access (e.g. an employee who can read but not write). The flows are also reachable                                                                                                                                                                         ↓a plain keyword ("create invoice") so writes work without AI.↓2.5 Webhook precedence (extends the existing ladder)

onboarding → active write-workflow → pending-operation confirm (YES/NO) → follow-up →                                                                                                                                                                                 ↓nu 1-4 → agent (read/chat). Onboarding-style: mid-flow a bare "10" is the quantityswer, not a menu command. Confirm re-validates at execute time (outstanding may have moved)d replies an error rather than committing a bad write.                                                                                                                                                                                                              ↓

Tests                                                                                                                                                                                                                                                                 ↓

Phase 1: test_agent_tools.py (each read tool → correct real numbers),                                                                                                                                                                                               ↓test_agent_runner.py (fake tool-provider: business Q drives tool call + grounded answer;greeting → no tool; money-gate catches hallucination; unexpected error → fallback; max-steps                                                                                                                                                                          ↓lts), test_assistant.py multi-turn + persistence.Phase 2: test_writes.py (create_invoice computes totals + persists items;cord_payment FIFO + status flip; over-outstanding rejected), test_pending_operation.py                                                                                                                                                                              ↓xecute dispatch; stale-outstanding errors at confirm), test_invoice_flow.py /st_payment_flow.py (full guided walk, bad-input re-asks, preview totals, YES commits / NOscards, post-invoice send offer), webhook precedence tests.Real-LLM smoke gated on a key.↓rification↓

alembic upgrade head, full pytest, ruff, pre-commit.

Scripted chat vs AP BIOCARE: "hi" → "how much does Siddha owe?" (₹3,19,828) → "and                                                                                                                                                                                 ↓liable?" (₹39,986) → "record payment" → guided (Siddha, 50000) → preview → YES →lance ₹2,69,828 → "create invoice" → guided (Reliable, Rice, 10, 500, 18%) → preview total,900 → YES → WA-invoice + items → "send to dealer?" flow.                                                                                                                                                                                                           ↓

Multi-angle review before each phase commit (money-safety, confirm-gate can't bebypassed, flow can't strand/double-write, agent never-raises, provider fallback).↓Notes / risks

Tool-capable model: free gpt-oss-120b supports tools but is flaky; gemini/groq                                                                                                                                                                                      ↓llbacks are reliable. Will flag if a sturdier primary is worth setting.Auto-send to dealer depends on the WhatsApp 24h window / an approved template for                                                                                                                                                                                   ↓siness-initiated messages — first cut offers + attempts + logs non-delivery.Deferred: add_dealer/add_supplier/cancel_invoice flows (same engine), role-based                                                                                                                                                                                    ↓rmission enforcement, per-company rate limiting, streaming.